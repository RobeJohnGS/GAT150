#include "Transform.h"

namespace JREngine {
	/*
	* bool Transform::Write(vonst rapidjson::Value& value) const {
		
		return true;
	}
	* 
	bool Transform::Read(vonst rapidjson::Value& value){
		READ_DATA(value, position);
		READ_DATA(value, scale);
		READ_DATA(value, rotation);
		return true;
	}
	*/
}